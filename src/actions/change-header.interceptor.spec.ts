import { ChangeHeaderInterceptor } from './change-header.interceptor';

describe('ChangeHeaderInterceptor', () => {
  it('should be defined', () => {
    expect(new ChangeHeaderInterceptor()).toBeDefined();
  });
});
